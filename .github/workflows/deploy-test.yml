name: Deploy - test

on:
  push:
    branches: [test]

jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: test
      build: false
    secrets: inherit
