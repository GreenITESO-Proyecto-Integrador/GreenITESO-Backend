name: Release - dev

on:
  push:
    branches: [dev]

jobs:
  release:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: dev
      release_sha: ${{ github.sha }}
      source_ref: dev
      build: true
    secrets: inherit
